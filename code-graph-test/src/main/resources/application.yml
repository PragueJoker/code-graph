server:
  port: 8082

spring:
  application:
    name: code-graph-test

# Code Graph Engine 配置
codegraph:
  engine:
    url: http://localhost:8081

# 日志配置
logging:
  level:
    root: INFO
    com.poseidon.codegraph.test: DEBUG

